@model DeliveryMonitoring.Models.OrderDetail

@{
    ViewData["Title"] = "Order Details";
    var phoneNumber = Model.AssignedDriverPhoneNumber;
}
<!-- Add this line to include the custom CSS -->
<link rel="stylesheet" href="~/css/orderDetail.css" />
<div class="container-fluid col-11 pt-5">

    <h3>Order Details</h3>

    <div class="card" id="orderCard">
        <br />      
        <div class="row">
            <div class="col-11 col-md-6" id="orderDetail">
                <div>
                    <span><strong>VoucherCode:</strong></span>
                    <span>@Html.DisplayFor(model => model.VoucherCode)</span>
                </div>
                <div>
                    <span><strong>Expected Time Of Arrival:</strong></span>
                    <span>@(Model?.Activities?.Eta?.ToString("ddd, MMMM dd - hh:mm:ss tt") ?? "N/A")</span>
                </div>
                <div>
                    <span><strong>Actual Arrival:</strong></span>
                    <span>@(Model?.Activities?.ActualArrival?.ToString("ddd, MMMM dd - hh:mm:ss tt") ?? "N/A")</span>
                </div>
                
                <div class="d-flex d-inline-block">
                    <div>
                        @{
                            var statusColors = new Dictionary<string, string>
                            {
                                { "requested", "deepskyblue" },
                                { "arrivedatbranch", "orange" },
                                { "assigned", "lawngreen" },
                                { "declined", "red" },
                                { "accepted", "seagreen" },
                                { "arrived", "coral" },
                                { "ontheway", "darkorange" },
                                { "drivernotfound", "red" },
                                { "sos", "darkred" },
                                { "default", "yellow" }
                            };
                            var bgColor = statusColors.ContainsKey(Model?.Status?.ToLower() ?? "") ? statusColors[Model?.Status?.ToLower() ?? ""] : statusColors["default"];
                        }
                        <div>
                            <span><strong>Order Status:</strong></span>
                            <div style="background-color: @bgColor;
                                border-radius: 10px;
                                color: @(bgColor == "red" || bgColor == "green" ? "white" : "black");
                                display: inline-block;
                                padding: 5px 5px;">
                                <strong>@Html.DisplayFor(model => model.Status) </strong>
                            </div>
                            <span class="text-info bg-aqua">@(Model?.Status == "sos" && Model.SosReason != null ? $"{Model.SosReason}" : "")</span>
                        </div>
                    </div>
                </div>
            </div>
            <div class="col-12 col-md-5 d-flex justify-content-end">
                <div class="d-flex align-items-end flex-column">
                    <div class="mb-1"> 
                        <span><strong>@Model?.CreatedAt?.ToString(" ddd, MMMM dd, yyyy- hh:mm:ss tt")</strong></span>
                    </div>
                    <div>
                        @{
                            if (phoneNumber != null && phoneNumber.Length < 10)
                            {
                                <div style="background-color: red;
                                    border-radius: 20px;
                                    color: black;
                                    display: inline-block;
                                    padding: 5px 5px;">
                                    <strong>Driver Phone Not Available</strong>
                                </div>
                            }
                            else
                            {
                                <a class="btn btn-dark" href="/driverdetail/@phoneNumber">Check Order Status</a>
                            }
                        }
                    </div>
                </div>
            </div>
        </div>
        <br />

        <div class="row">

            <!--Customer Details-->
            <div class="col-12 col-md-4 detail" id="orderDetail">
                <h4><i class="bi bi-person-circle icon"></i>Customer</h4>
                <div>
                    <span id="letters">Name:</span>
                    <span id="outputs">@Model?.Customer?.FirstName</span>
                </div>
                <div>
                    <span id="letters">Address:</span>
                    <span id="outputs">
                        @Model?.CustomerGeocodeAddress -
                        @Model?.CustomerSpecificAddress
                    </span>
                </div>
                <div>
                    <span id="letters">Phone:</span>
                    <span id="outputs">@Model.CustomerPhoneNumber</span>
                </div>
                @* <div>
                <span id="letters">Device ID:</span>
                <span id="outputs">@Html.DisplayFor(model => model.customer.deviceId)</span>
                </div> *@
            </div>
            <!--Customer Details Ends Here-->
            <!--Company Details-->
            <div class="col-12 col-md-4 detail">
                <h4><i class="bi bi-building icon"></i>Company</h4>
                <div>
                    <span id="letters">Branch:</span>
                    <span id="outputs">@Html.DisplayFor(model => model.BranchName)</span>
                </div>
                <div>
                    <span id="letters">Name:</span>
                    <span id="outputs">@Html.DisplayFor(model => model.CompanyName)</span>
                </div>
                <div>
                    <span id="letters">TIN:</span>
                    <span id="outputs">
                        @* <a asp-controller="Company" asp-action="Details" asp-route-id="@Model.companyTin">@Html.DisplayFor(model => model.companyTin)</a> *@
                        <a>@Html.DisplayFor(model => model.CompanyTin)</a>
                    </span>
                </div>
            </div>
            <!--Company Details Ends Here-->
            <!--Driver Details-->
            <div class="col-12 col-md-3">
                <h4><i class="bi bi-car-front icon"></i>Driver</h4>
                <div>
                    <span id="letters">Phone:</span>
                    <span id="outputs">
                        @Html.DisplayFor(model => model.AssignedDriverPhoneNumber)
                    </span>
                </div>
            </div>
            <!--Driver Details Ends Here-->
        </div>
        <!--Order Items Table-->
        <h4 id="orderDetail">Products</h4>
        <div class="col-11 table-responsive table-bordered" id="orderDetail">
            <table class="table pt-3" id="orderTable">
                <thead class="table-secondary">
                    <tr>
                        <th>
                            Item Name
                        </th>
                        <th>
                            Quantity
                        </th>
                        <th>
                            Unit Amount
                        </th>
                        <th>
                            Taxable Amount
                        </th>
                    </tr>
                </thead>
                <tbody>
                    @foreach (var item in Model?.LineItemsDetail?.LineItems ?? new List<LineItem>())
                    {
                        <tr>
                            <td>
                                @Html.DisplayFor(modelItem => item.Name)
                            </td>
                            <td>
                                @Html.DisplayFor(modelItem => item.Quantity)
                            </td>
                            <td>
                                @String.Format("{0:F2} ETB", item.UnitAmount)
                            </td>
                            <td>
                                @String.Format("{0:F2} ETB", item.TaxableAmount)
                            </td>
                        </tr>
                    }
                    @foreach (var charge in Model?.LineItemsDetail?.ExtraCharge ?? new Dictionary<string, decimal>())
                    {
                        <tr>
                            <td>
                                @charge.Key
                            </td>
                            <td>
                            </td>
                            <td>
                            </td>
                            <td>
                                @String.Format("{0:F2} ETB", @charge.Value)
                            </td>
                        </tr>
                    }
                    <tr class="table-secondary bold-borders">
                        <td>
                            <h4><b>TOTAL</b></h4>
                        </td>
                        <td>
                        </td>
                        <td>
                        </td>
                        <td>
                            <h4><b>@String.Format("{0:F2} ETB", Model?.LineItemsDetail?.GrandTotal)</b></h4>
                        </td>
                    </tr>
                </tbody>
            </table>
            <br/>
        </div>
    </div>
</div>
<div class="uk-container uk-padding">
    <div class="uk-timeline">
        @foreach (var activity in Model?.Activities?.ActivityResponse ?? new List<ActivityResponse>())
        {
            <div class="uk-timeline-item">
                <div class="uk-timeline-icon">
                    <span class="uk-badge"><span uk-icon="check"></span></span>
                </div>
                <div class="uk-timeline-content">
                    <div class="uk-card uk-card-default uk-margin-medium-bottom uk-overflow-auto">
                        <div class="uk-card-header">
                            <div class="uk-grid-small uk-flex-middle" uk-grid>

                                <!-- Column 1: Date and Time -->
                                <div class="uk-width-1-3">
                                    <p class="uk-card-title uk-margin-remove-bottom">
                                        <time datetime="@activity?.Time?.ToString("yyyy-MM-ddTHH:mm:ss")">
                                            @activity?.Time?.ToString("ddd MMM, dd yyyy hh:mm:ss tt")
                                        </time>
                                    </p>
                                </div>

                                <!-- Column 2: Elapsed Time -->
                                <div class="uk-width-1-3 uk-text-center">
                                    @if (!string.IsNullOrEmpty(activity?.TimeElapsed))
                                    {
@*                                         <span class="uk-label uk-label-warning">@FormatElapsedTime(activity.TimeElapsed)</span>
 *@                                        <span class="uk-label uk-label-warning">@activity.TimeElapsed</span>
                                    }
                                </div>

                                <!-- Column 3: Activity Name -->
                                <div class="uk-width-1-3 uk-text-right">
                                    <span class="uk-label uk-label-success">@activity?.Name</span>
                                </div>

                            </div>
                        </div>
                    </div>
                </div>
            </div>
        }
    </div>
</div>
@functions {
    public string FormatElapsedTime(string timeElapsed)
    {
        if (string.IsNullOrWhiteSpace(timeElapsed)) return "N/A";

        // Remove "Minutes" and trim spaces
        var cleaned = timeElapsed.Replace("Minutes", "").Trim();

        // Parse the number of minutes
        if (!double.TryParse(cleaned, out double minutes))
        {
            return "Invalid Time";
        }

        // Convert total minutes to total seconds and round up to the nearest whole second
        var totalSeconds = (int)Math.Ceiling(minutes * 60);

        // Calculate hours, minutes, and seconds
        int hours = totalSeconds / 3600;
        int mins = (totalSeconds % 3600) / 60;
        int secs = totalSeconds % 60;

        // Build readable string
        var parts = new List<string>();
        if (hours > 0) parts.Add($"{hours}h");
        if (mins > 0) parts.Add($"{mins}m");
        if (secs > 0) parts.Add($"{secs}s");

        // If all parts are zero (unlikely, but for safety), return "0s"
        return parts.Count > 0 ? string.Join(" ", parts) : "0s";
    }
}