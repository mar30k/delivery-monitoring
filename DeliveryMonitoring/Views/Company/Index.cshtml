@model DeliveryMonitoring.Models.CompanyIndex

@{
    ViewData["Title"] = "Companies List";
}

<div class="container-fluid pt-5 col-11">

    <h3>List of Companies</h3>
    <div class="card pt-3 pb-3 row">
        <table class="table compact stripe hover pt-3 col-11" id="tablelist">
            <thead class="">
                <tr>
                    <th>Company Name</th>
                    <th>
                        Company TIN
                    </th>
                    <th>Company Details</th>
                    <th>Check Drivers</th>
                </tr>
            </thead>
            <tbody>
                @{
                    @foreach (var tin in Model.Companies.companyTins)
                    {
                        var company = Model.company.FirstOrDefault(c => c?.Tin == tin);

                        <tr>
                            <td>@(company?.BrandName ?? "N/A")</td>
                            <td>@(tin ?? "N/A")</td>
                            <td>
                                <a class="btn btn-outline-dark" href="/company/@tin">Details</a>
                            </td>
                            <td>
                                <a href="Drivers/FilterCompany/@tin">Drivers</a>
                            </td>
                        </tr>
                    }
                }
            
            </tbody>
        </table>
    </div>


    <!--Jquery Datatable Starts Here-->
    <script src="~/lib/jquery/dist/jquery.min.js"></script>
    <link rel="stylesheet" href="https://cdn.datatables.net/1.13.6/css/jquery.dataTables.css" />
    <script src="https://cdn.datatables.net/1.13.6/js/jquery.dataTables.js"></script>
    <script>
        var js = jQuery.noConflict(true);
        js(document).ready(function () {
            js('#tablelist').DataTable({
                responsive: true,
                columnDefs: [
                    { orderable: false, targets: [2, 3] }
                ]
            });
        });
    </script>
    <!--Jquery Datatable Ends Here-->
</div>

